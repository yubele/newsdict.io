#!/usr/bin/env bash
set -e
if [ -f tmp/pids/server.pid ];then
  sudo rm -f tmp/pids/server.pid
fi
if [ "$1" = "rm" ];then
  shift
  docker-compose rm -f
fi
docker-compose stop
if [ "$1" = "debug" ]; then
  docker-compose up -d
  docker attach newsdict-web
else
  docker-compose up $@
fi