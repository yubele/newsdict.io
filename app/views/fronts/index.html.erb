<%= render partial: "nav" %>
<section class="section main">
    <div class="columns">
      <div class="column is-2">
      </div>
      <div class="column is-8">
        <div class="box">
          <% @contents.each do |content| %>
          <article class="media">
            <div class="media-left">
              <figure class="image is-64x64">
                <img src="http://placehold.it/128x128" alt="Image">
              </figure>
            </div>
            <div class="media-content">
              <h2><strong><%= content.title %></strong></h2>
              <div class="content">
                  <%= content.content.truncate(100) %>
              </div>
              <hr>
              <% content.tags.first(5).each do |tag| %>
                <span class="tag is-dark"><%= tag %></span>
              <% end %>
              <p class="is-pulled-right">
                <%= t('shared by') %> <strong><%= find_name_by_source(content.source_id) %></strong> / <small><%= Time.at(content.created_at).in_time_zone %></small>
              </p>
          </article>
          <% end %>
        </div>
      </div>
      <div class="column is-2">
      </div>
  </div>
</section>